
#include "slackpipeline.h"
using namespace elabtsaot;

SlackPipeline::SlackPipeline(size_t element_capacity,
                             size_t ver_dim, size_t hor_dim) :
  Pipeline(element_capacity, ver_dim, hor_dim){}
